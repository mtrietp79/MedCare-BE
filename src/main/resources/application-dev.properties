# Dev profile: bật khi chạy demo local
# java -jar app.jar --spring.profiles.active=dev
# hoặc SPRING_PROFILES_ACTIVE=dev

spring.jpa.show-sql=true

# OTP reset password sẽ được log ra console (PasswordService)
logging.level.com.medcare.clinic_backend.service.PasswordService=INFO

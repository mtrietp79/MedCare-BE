spring.application.name=clinic-backend

# PostgreSQL
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/clinic_booking_db}
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:@Bon12345}

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.open-in-view=false

# VNPay
vnpay.tmnCode=${VNPAY_TMN_CODE:R44OLUCC}
vnpay.hashSecret=${VNPAY_HASH_SECRET:HVJOEMC7U9RKQBMTBJVH0G4X8RIMSUTR}
vnpay.payUrl=${VNPAY_PAY_URL:https://sandbox.vnpayment.vn/paymentv2/vpcpay.html}
vnpay.returnUrl=${VNPAY_RETURN_URL:http://localhost:8080/api/payment/vnpay-return}
vnpay.appointmentFrontendReturnUrl=${VNPAY_APPOINTMENT_FRONTEND_RETURN_URL:http://localhost:5174/payment/vnpay-result}
vnpay.servicePackageFrontendReturnUrl=${VNPAY_SERVICE_PACKAGE_FRONTEND_RETURN_URL:${VNPAY_APPOINTMENT_FRONTEND_RETURN_URL:http://localhost:5174/payment/vnpay-result}}
vnpay.invoiceFrontendReturnUrl=${VNPAY_INVOICE_FRONTEND_RETURN_URL:${VNPAY_APPOINTMENT_FRONTEND_RETURN_URL:http://localhost:5174/payment/vnpay-result}}

# Mail (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:trietminhpham79@gmail.com}
spring.mail.password=${MAIL_PASSWORD:vqet bvod qlik bmlf}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# JWT
jwt.secret=${JWT_SECRET:medcare-clinic-default-secret-key-32chars!!}
jwt.expiration=${JWT_EXPIRATION:86400000}

# SMS OTP provider (optional)
app.sms.enabled=${SMS_ENABLED:false}
app.sms.provider-url=${SMS_PROVIDER_URL:}
app.sms.api-key=${SMS_API_KEY:}
app.otp.phone-dev-mode=${OTP_PHONE_DEV_MODE:false}
app.otp.expose-dev-otp=${OTP_EXPOSE_DEV_OTP:false}

server.port=8080

# Default admin account (chi tao neu chua ton tai)
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD:Admin@123456}

# Doctor photo upload
spring.servlet.multipart.max-file-size=${MULTIPART_MAX_FILE_SIZE:2MB}
spring.servlet.multipart.max-request-size=${MULTIPART_MAX_REQUEST_SIZE:2MB}
app.doctor-photo.max-size-bytes=${DOCTOR_PHOTO_MAX_SIZE_BYTES:2097152}
app.medical-service-photo.max-size-bytes=${MEDICAL_SERVICE_PHOTO_MAX_SIZE_BYTES:2097152}
